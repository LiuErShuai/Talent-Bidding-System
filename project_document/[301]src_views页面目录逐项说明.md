<!--
时间戳：2025-12-18 13:00:49
文档编号：301（技术专题文档）
-->

# [301] src/views 页面目录逐项说明

## 背景与目标
项目页面集中在 `src/views/` 下，按角色与业务域拆分为 `admin`、`enterprise`、`home`、`info`、`message`、`project`、`student`、`user` 等目录。为了便于协作与快速定位页面职责，需要对各目录下页面文件做逐项说明，明确其用途、主要交互与关键依赖（路由、Store、组件）。

**目标：**
1. 列出上述目录下的所有页面文件（以 `*.vue` 为主）。
2. 对每个页面文件给出：页面用途、核心模块/组件、关键逻辑点与路由关联。
3. 输出一份可扫描的“页面地图”，供后续功能迭代参考。

## 目录
1. 范围与口径
2. 详细 todolist（时间戳）
3. 记录（等效 memory）

## 1. 范围与口径
- 说明对象：`src/views/<folder>/**` 下的页面文件（主要是 `index.vue` 或其他 `.vue`）。
- 说明口径：不展开逐行代码讲解，以“功能与职责”为主，必要时补充关键变量/事件/接口调用点。
- 若页面文件数量较多，优先保证“逐个文件都有说明”，深度按重要性分层。

## 2. 详细 todolist（时间戳）
1.（2025-12-18 13:00:49）扫描 `src/views/` 目录结构，按文件夹统计页面文件清单。
2.（2025-12-18 13:00:49）逐文件阅读：提取用途、主要组件引用、关键业务逻辑与交互事件。
3.（2025-12-18 13:00:49）按 `admin/enterprise/home/info/message/project/student/user` 分组输出说明。
4.（完成后补充）将关键结论与约定写入“记录（等效 memory）”，便于追溯。

## 3. 记录（等效 memory）
- 2025-12-18 13:00:49：用户要求对 `src/views` 各业务目录页面文件逐个解释；本项目当前环境未直接暴露 `memory` MCP 工具时，使用本文件作为等效记录载体，沉淀页面地图与关键决策。
- 2025-12-18 13:02:14：已完成页面文件清单扫描（共 23 个 `.vue` 页面），并在对话中按目录逐个解释页面职责与关键依赖。
