# 个人主页资料显示代码提取 - 样式代码

## 1. CSS变量系统（核心部分）

个人资料显示组件依赖的CSS变量系统。这些变量定义了主题色彩、间距、字体等设计令牌。

### 完整CSS变量定义

```css
/* ==========================================================================
   CSS变量定义 - 基于原型设计
   ========================================================================== */

:root {
  /* 色彩系统 */
  --color-primary: #1890ff;
  --color-primary-hover: #40a9ff;
  --color-primary-active: #096dd9;
  --color-success: #52c41a;
  --color-warning: #faad14;
  --color-error: #f5222d;
  --color-neutral: #f0f2f5;
  --color-neutral-dark: #1f2937;

  /* 文字颜色 */
  --text-color-primary: #262626;
  --text-color-secondary: #595959;
  --text-color-disabled: #bfbfbf;
  --text-color-inverse: #ffffff;
  --text-color-tertiary: #8c8c8c;

  /* 背景颜色 */
  --bg-color-page: #ffffff;
  --bg-color-container: #ffffff;
  --bg-color-elevated: #ffffff;
  --bg-color-overlay: rgba(0, 0, 0, 0.45);
  --bg-color-hover: rgba(24, 144, 255, 0.05);

  /* 其他色彩 */
  --color-primary-light: rgba(24, 144, 255, 0.1);

  /* 边框 */
  --border-color: #d9d9d9;
  --border-color-hover: #40a9ff;
  --border-color-dark: #434343;
  --border-radius: 6px;
  --border-radius-sm: 4px;
  --border-radius-md: 8px;
  --border-radius-lg: 12px;
  --border-radius-full: 9999px;

  /* 阴影 */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.03), 0 1px 6px -1px rgba(0, 0, 0, 0.02), 0 2px 4px 0 rgba(0, 0, 0, 0.02);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);

  /* 布局 */
  --header-height: 64px;
  --sidebar-width: 256px;
  --content-max-width: 1200px;

  /* 字体 */
  --font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-md: 16px;
  --font-size-lg: 18px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 30px;
  --font-size-4xl: 36px;

  /* 间距 */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;

  /* 过渡动画 */
  --transition-fast: 0.15s cubic-bezier(0.4, 0, 0.2, 1);
  --transition-normal: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 暗色模式变量 */
.dark {
  --text-color-primary: #f0f0f0;
  --text-color-secondary: #bfbfbf;
  --text-color-disabled: #595959;

  --bg-color-page: #141414;
  --bg-color-container: #1f1f1f;
  --bg-color-elevated: #262626;
  --bg-color-overlay: rgba(255, 255, 255, 0.15);

  --border-color: #434343;
  --border-color-hover: #40a9ff;
  --border-color-dark: #434343;

  --bg-color-hover: rgba(255, 255, 255, 0.05);
  --bg-color-hover-dark: rgba(255, 255, 255, 0.05);
  --color-primary-dark: rgba(64, 169, 255, 0.15);

  --color-neutral: var(--color-neutral-dark);
}
```

## 2. 组件样式集合

所有个人资料显示组件使用的完整样式代码。

### ProfileCard组件样式

```css
/* 简历档案样式 */
.profile-card {
  margin: var(--spacing-lg) 0;
}

.profile-card .card-header h2 {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  margin: 0;
}

.profile-card .card-header h2 i {
  color: var(--color-primary);
}

.profile-section {
  margin: var(--spacing-xl) 0;
}

.profile-section h3 {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-lg);
  color: var(--text-color-primary);
  font-weight: 600;
  border-bottom: 2px solid var(--color-primary-light);
  padding-bottom: var(--spacing-sm);
}

.profile-section h3 i {
  color: var(--color-primary);
}

/* 基本信息样式 */
.not-available {
  color: var(--text-color-disabled);
  font-style: italic;
}

/* 技能标签样式 */
.skill-tag {
  margin-right: var(--spacing-sm);
  margin-bottom: var(--spacing-sm);
}

.skill-tag i {
  margin-right: var(--spacing-xs);
}

/* 工作经验样式 */
.experience-item h4 {
  color: var(--text-color-primary);
  margin-bottom: var(--spacing-sm);
}

.experience-item p {
  color: var(--text-color-secondary);
  margin-bottom: var(--spacing-sm);
}

.achievements h5 {
  color: var(--text-color-primary);
  margin: var(--spacing-sm) 0 var(--spacing-xs) 0;
  font-size: var(--font-size-sm);
}

.achievements ul {
  margin: 0;
  padding-left: var(--spacing-lg);
}

.achievements li {
  color: var(--text-color-secondary);
  margin-bottom: var(--spacing-xs);
}

/* 项目经验样式 */
.project-item {
  margin-bottom: 1.5rem;
  padding: 1rem;
  border: 1px solid var(--border-color);
  border-radius: 8px;
  background: var(--bg-color-elevated);
}

.project-title {
  color: var(--text-color-primary);
  margin-bottom: 0.5rem;
  font-size: 1.1rem;
}

.project-content {
  margin: 1rem 0;
}

.project-description h5 {
  color: var(--text-color-secondary);
  font-size: 0.9rem;
  margin-bottom: 0.5rem;
}

.keywords-label {
  color: var(--text-color-secondary);
  font-size: 0.9rem;
  margin-right: 0.5rem;
}

.keyword-tag {
  margin-right: 0.5rem;
  margin-bottom: 0.3rem;
}

/* 教育背景样式 */
.education-item {
  line-height: 1.6;
}

.education-item strong {
  color: var(--text-color-primary);
}

.education-achievements {
  margin-top: var(--spacing-sm);
}

.education-achievements .el-tag {
  margin-right: var(--spacing-sm);
  margin-bottom: var(--spacing-xs);
}

/* 额外信息样式 */
.extra-info-item h4 {
  margin: 0 0 var(--spacing-sm) 0;
  color: var(--text-color-primary);
  font-size: var(--font-size-md);
}

.extra-info-item .el-tag {
  margin-right: var(--spacing-sm);
  margin-bottom: var(--spacing-xs);
}

.extra-info-item .el-tag i {
  margin-right: var(--spacing-xs);
}
```

### UserHeader组件样式

```css
.user-dropdown {
  cursor: pointer;
}

.user-avatar {
  display: flex;
  align-items: center;
  padding: var(--spacing-sm);
  border-radius: var(--border-radius);
  transition: background-color var(--transition-normal);
}

.user-avatar:hover {
  background-color: rgba(24, 144, 255, 0.05);
}

.user-name {
  margin: 0 var(--spacing-sm);
  font-weight: 500;
}

.user-avatar i {
  color: var(--text-color-secondary);
  font-size: var(--font-size-xs);
}

/* 响应式设计 */
@media (max-width: 767px) {
  .user-name {
    display: none;
  }
}
```

### SettingsForm组件样式

```css
.settings-form {
  padding: var(--spacing-lg);
}

.settings-card {
  margin-bottom: var(--spacing-lg);
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: var(--spacing-md);
  margin-top: var(--spacing-lg);
}
```

### SimpleProfileDisplay组件样式

```css
.simple-profile-card {
  margin-bottom: var(--spacing-lg);
}

.profile-header {
  display: flex;
  align-items: center;
  gap: var(--spacing-lg);
}

.profile-info h3 {
  margin: 0 0 var(--spacing-xs) 0;
  font-size: var(--font-size-xl);
  font-weight: 600;
  color: var(--text-color-primary);
}

.profile-email {
  margin: 0;
  color: var(--text-color-secondary);
  font-size: var(--font-size-sm);
}
```

## 3. 全局样式重置（可选）

如果需要完整的样式重置，可以使用以下代码：

```css
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  line-height: 1.5;
}

body {
  font-family: var(--font-family);
  font-size: var(--font-size-md);
  line-height: 1.6;
  color: var(--text-color-primary);
  background-color: var(--bg-color-page);
  transition: color var(--transition-normal), background-color var(--transition-normal);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```

## 4. 图标支持

组件使用了FontAwesome图标，需要在HTML中引入：

```html
<!-- 在index.html或main.js中引入 -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
```

或者使用Element Plus图标（推荐）：

```javascript
import { User, Setting, Phone, Envelope, GraduationCap } from '@element-plus/icons-vue'
```

---

**继续阅读**: 下一部分包含完整的使用指导和集成说明。

